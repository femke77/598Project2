package project;
/*

package project2;

import lejos.robotics.Color;
import lejos.robotics.SampleProvider;
import lejos.robotics.filter.AbstractFilter;

//change to public if you use this..... right now its inside Movement
private class ColorSensor extends AbstractFilter {
    
	float sample[];
	
	public ColorSensor(SampleProvider source) {
		super(source);
		sample = new float[sampleSize];
	}

	
	public boolean black(){
		super.fetchSample(sample, 0);
		if (sample[0] == Color.BLACK){ //black
			return true;
		}
		else{ //not black, anything else
			return false;
		}
	}
	
	
	
	
}


*/